services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: trinketos-api-preview
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env.preview
 